"use client";
import Link from "next/link";import {FormEvent,Suspense,useState} from "react";import {useSearchParams} from "next/navigation";import {api} from "@/lib/api";import {Button} from "@/components/ui/button";import {Field,Input} from "@/components/ui/input";import {ErrorMessage,Loading} from "@/components/ui/feedback";
function Reset(){const params=useSearchParams();const [token,setToken]=useState(params.get("token")||"");const [password,setPassword]=useState("");const [confirm,setConfirm]=useState("");const [error,setError]=useState("");const [done,setDone]=useState(false);const [busy,setBusy]=useState(false);async function submit(event:FormEvent){event.preventDefault();if(password!==confirm){setError("Passwords do not match");return}setBusy(true);setError("");try{await api("/auth/reset-password",{method:"POST",body:JSON.stringify({token,password})});setDone(true)}catch(e){setError(e instanceof Error?e.message:"Unable to reset password")}finally{setBusy(false)}}return <div className="w-full max-w-md"><p className="text-xs font-semibold uppercase tracking-[.16em] text-[#167d78]">Secure password setup</p><h1 className="mt-3 text-3xl font-semibold tracking-[-.03em]">Choose a new password</h1>{done?<div className="mt-7"><div className="border-l-4 border-[#267a52] bg-[#f0f8f3] p-4 text-sm text-[#315c46]">Password updated. Existing sessions were revoked.</div><Link href="/login" className="mt-5 inline-flex text-sm font-semibold text-[#0f625f]">Continue to sign in</Link></div>:<form onSubmit={submit} className="mt-8 space-y-5"><ErrorMessage message={error}/>{!params.get("token")&&<Field label="Reset token" required><Input value={token} onChange={e=>setToken(e.target.value)} required/></Field>}<Field label="New password" required hint="At least 8 characters"><Input type="password" minLength={8} autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)} required/></Field><Field label="Confirm password" required><Input type="password" minLength={8} autoComplete="new-password" value={confirm} onChange={e=>setConfirm(e.target.value)} required/></Field><Button className="w-full" disabled={busy}>{busy?"Updating…":"Set password and revoke sessions"}</Button></form>}</div>}
export default function ResetPage(){return <Suspense fallback={<Loading/>}><Reset/></Suspense>}
