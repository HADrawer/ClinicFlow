"use client";import Link from "next/link";import {FormEvent,useState} from "react";import {useAuth} from "@/lib/auth";import {Button} from "@/components/ui/button";import {Field,Input} from "@/components/ui/input";import {ErrorMessage} from "@/components/ui/feedback";
export default function Login(){const {login}=useAuth();const [email,setEmail]=useState("owner@clinicflow.test");const [password,setPassword]=useState("password123");const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");try{await login(email,password)}catch(e){setError(e instanceof Error?e.message:"Unable to sign in");setBusy(false)}}return <div className="w-full max-w-md"><p className="text-xs font-semibold uppercase tracking-[.16em] text-[#167d78]">Secure clinic access</p><h1 className="mt-3 text-3xl font-semibold tracking-[-.03em] text-[#10212b]">Return to the current</h1><p className="mt-2 text-sm text-[#52656e]">Sign in with the account assigned by your clinic owner.</p><form onSubmit={submit} className="mt-8 space-y-5"><ErrorMessage message={error}/><Field label="Email address" required><Input type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required/></Field><div><Field label="Password" required><Input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required/></Field><div className="mt-2 text-right"><Link className="text-xs font-semibold text-[#0f625f] hover:underline" href="/forgot-password">Forgot password?</Link></div></div><Button className="w-full" disabled={busy}>{busy?"Signing in…":"Sign in securely"}</Button></form><div className="mt-6 border-l-4 border-[#167d78] bg-[#f1f7f5] p-4 text-xs leading-5 text-[#526973]"><strong className="text-[#314854]">Demo owner</strong><br/>owner@clinicflow.test · password123</div><p className="mt-6 text-center text-sm text-[#52656e]">Opening a clinic? <Link className="font-semibold text-[#0f625f] hover:underline" href="/register">Create your workspace</Link></p></div>}
