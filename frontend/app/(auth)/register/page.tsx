"use client";import Link from "next/link";import {FormEvent,useState} from "react";import {useAuth} from "@/lib/auth";import {Button} from "@/components/ui/button";import {Field,Input} from "@/components/ui/input";import {ErrorMessage} from "@/components/ui/feedback";
export default function Register(){const {register}=useAuth();const [data,setData]=useState({clinic_name:"",full_name:"",email:"",phone:"+973 ",password:""});const [error,setError]=useState("");const [busy,setBusy]=useState(false);const set=(k:string,v:string)=>setData(x=>({...x,[k]:v}));async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");try{await register(data)}catch(e){setError(e instanceof Error?e.message:"Unable to register");setBusy(false)}}return <div className="w-full max-w-lg"><p className="text-sm font-semibold text-[#1d4f91]">ClinicFlow</p><h1 className="mt-3 text-3xl font-bold tracking-tight text-slate-950">Create your clinic workspace</h1><p className="mt-2 text-sm text-slate-500">Set up the owner account. Staff can be added later.</p><form onSubmit={submit} className="mt-7 grid gap-4 sm:grid-cols-2"><div className="sm:col-span-2"><ErrorMessage message={error}/></div><div className="sm:col-span-2"><Field label="Clinic name" required><Input value={data.clinic_name} onChange={e=>set("clinic_name",e.target.value)} required/></Field></div><Field label="Owner name" required><Input value={data.full_name} onChange={e=>set("full_name",e.target.value)} required/></Field><Field label="Clinic phone" required><Input value={data.phone} onChange={e=>set("phone",e.target.value)} required/></Field><div className="sm:col-span-2"><Field label="Work email" required><Input type="email" value={data.email} onChange={e=>set("email",e.target.value)} required/></Field></div><div className="sm:col-span-2"><Field label="Password" required hint="At least 8 characters"><Input type="password" minLength={8} value={data.password} onChange={e=>set("password",e.target.value)} required/></Field></div><div className="sm:col-span-2"><Button className="w-full" disabled={busy}>{busy?"Creating workspace…":"Create workspace"}</Button></div></form><p className="mt-5 text-center text-sm text-slate-500">Already registered? <Link className="font-semibold text-blue-700 hover:underline" href="/login">Sign in</Link></p></div>}
