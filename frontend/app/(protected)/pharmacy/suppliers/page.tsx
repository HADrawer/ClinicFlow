"use client";import {FormEvent,useState} from "react";import {Plus} from "lucide-react";import {api} from "@/lib/api";import {useApi} from "@/lib/hooks";import {PageHeader} from "@/components/ui/page-header";import {Card} from "@/components/ui/card";import {Button} from "@/components/ui/button";import {Badge} from "@/components/ui/badge";import {DataTable} from "@/components/ui/data-table";import {Modal} from "@/components/ui/modal";import {Field,Input} from "@/components/ui/input";import {ErrorMessage,Loading} from "@/components/ui/feedback";
type Supplier={id:number;name:string;phone?:string;email?:string;active:boolean};export default function Suppliers(){const suppliers=useApi<Supplier[]>("/pharmacy/suppliers");const [open,setOpen]=useState(false);if(suppliers.loading)return <Loading/>;return <><PageHeader title="Pharmacy suppliers" description="Purchasing contacts scoped to this clinic." actions={<Button onClick={()=>setOpen(true)}><Plus size={16}/>Add supplier</Button>}/><ErrorMessage message={suppliers.error}/><Card><DataTable rows={suppliers.data||[]} keyOf={item=>item.id} columns={[{key:"name",header:"Supplier",render:item=><strong>{item.name}</strong>},{key:"phone",header:"Phone",render:item=>item.phone||"—"},{key:"email",header:"Email",render:item=>item.email||"—"},{key:"status",header:"Status",render:item=><Badge value={item.active?"accepted":"revoked"}/>}]} /></Card><Modal open={open} onClose={()=>setOpen(false)} title="Add supplier" size="max-w-md"><SupplierForm onCancel={()=>setOpen(false)} onSaved={()=>{setOpen(false);suppliers.reload()}}/></Modal></>}
function SupplierForm({onCancel,onSaved}:{onCancel:()=>void;onSaved:()=>void}){const [form,setForm]=useState({name:"",phone:"",email:""});const [error,setError]=useState("");async function submit(event:FormEvent){event.preventDefault();setError("");try{await api("/pharmacy/suppliers",{method:"POST",body:JSON.stringify({...form,phone:form.phone||null,email:form.email||null})});onSaved()}catch(e){setError(e instanceof Error?e.message:"Unable to add supplier")}}return <form className="space-y-4 p-5" onSubmit={submit}><ErrorMessage message={error}/><Field label="Supplier name" required><Input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></Field><Field label="Phone"><Input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></Field><Field label="Email"><Input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></Field><div className="flex justify-end gap-2"><Button type="button" variant="secondary" onClick={onCancel}>Cancel</Button><Button>Add supplier</Button></div></form>}
