import {cleanup,render,screen} from "@testing-library/react";import {afterEach,beforeEach,describe,expect,it,vi} from "vitest";import {AppShell} from "./app-shell";
const auth=vi.fn();vi.mock("@/lib/auth",()=>({useAuth:()=>auth()}));vi.mock("next/navigation",()=>({usePathname:()=>"/dashboard",useRouter:()=>({replace:vi.fn()})}));
const user={id:1,clinic_id:1,email:"user@test",full_name:"Test User",role:"owner",is_active:true,permissions:[],clinic:{id:1,name:"Test Clinic",address:"",phone:"",working_hours:{},pharmacy_enabled:false,feature_flags:{}}};
describe("AppShell permissions",()=>{afterEach(()=>cleanup());beforeEach(()=>auth.mockReturnValue({user,loading:false,logout:vi.fn()}));it("hides disabled pharmacy navigation",async()=>{render(<AppShell><div>Content</div></AppShell>);expect(screen.queryByRole("link",{name:"Pharmacy"})).not.toBeInTheDocument();expect(await screen.findByRole("link",{name:"Staff access"})).toBeVisible()});it("shows pharmacy only when tenant feature is enabled",async()=>{auth.mockReturnValue({user:{...user,clinic:{...user.clinic,pharmacy_enabled:true}},loading:false,logout:vi.fn()});render(<AppShell><div>Content</div></AppShell>);expect(await screen.findByRole("link",{name:"Pharmacy"})).toBeVisible()})});
