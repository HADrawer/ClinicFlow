"use client";import {useEffect,useId,useRef} from "react";import {X} from "lucide-react";import {Button} from "./button";
export function Modal({open,onClose,title,children,size="max-w-2xl"}:{open:boolean;onClose:()=>void;title:string;children:React.ReactNode;size?:string}){const titleId=useId();const panel=useRef<HTMLDivElement>(null);useEffect(()=>{if(!open)return;const previous=document.activeElement as HTMLElement|null;const key=(event:KeyboardEvent)=>{if(event.key==="Escape")onClose()};document.addEventListener("keydown",key);requestAnimationFrame(()=>panel.current?.focus());return()=>{document.removeEventListener("keydown",key);previous?.focus()}},[open,onClose]);if(!open)return null;return <div className="fixed inset-0 z-50 flex items-center justify-center bg-[#10212b]/45 p-3 sm:p-4" onMouseDown={onClose}><div ref={panel} tabIndex={-1} role="dialog" aria-modal="true" aria-labelledby={titleId} className={`max-h-[92vh] w-full ${size} overflow-y-auto rounded-[6px] border border-[#b9cbc6] bg-white shadow-2xl outline-none`} onMouseDown={e=>e.stopPropagation()}><div className="sticky top-0 z-10 flex items-center justify-between border-b border-[#d6e1de] bg-white px-5 py-4"><h2 id={titleId} className="font-semibold text-[#10212b]">{title}</h2><button onClick={onClose} className="rounded-[3px] p-1 text-[#52656e] hover:bg-[#edf3f1]" aria-label="Close dialog"><X size={20}/></button></div>{children}</div></div>}
export function ConfirmDialog({open,onClose,onConfirm,title,description,busy}:{open:boolean;onClose:()=>void;onConfirm:()=>void;title:string;description:string;busy?:boolean}){return <Modal open={open} onClose={onClose} title={title} size="max-w-md"><div className="p-5"><p className="text-sm text-slate-600">{description}</p><div className="mt-6 flex justify-end gap-2"><Button variant="secondary" onClick={onClose}>Cancel</Button><Button variant="danger" disabled={busy} onClick={onConfirm}>{busy?"Working…":"Confirm"}</Button></div></div></Modal>}
