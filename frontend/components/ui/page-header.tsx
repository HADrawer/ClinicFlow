"use client";
import {translateEnglish,useI18n} from "@/lib/i18n";
export function PageHeader({title,description,actions}:{title:string;description?:string;actions?:React.ReactNode}){const {locale}=useI18n();return <div className="mb-5 flex flex-col justify-between gap-3 border-b border-[var(--line)] pb-4 sm:flex-row sm:items-end"><div className="min-w-0"><h1 className="text-[1.55rem] font-bold tracking-[-.018em] text-[var(--ink-950)]">{translateEnglish(title,locale)}</h1>{description&&<p className="mt-1 max-w-3xl text-sm leading-5 text-[var(--ink-500)]">{translateEnglish(description,locale)}</p>}</div>{actions&&<div className="flex shrink-0 flex-wrap items-center gap-2">{actions}</div>}</div>}
