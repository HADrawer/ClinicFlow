"use client";
import {InputHTMLAttributes,TextareaHTMLAttributes} from "react";import {cn} from "@/lib/utils";
import {translateEnglish,useI18n} from "@/lib/i18n";
export function Input({className,...props}:InputHTMLAttributes<HTMLInputElement>){return <input className={cn("control h-10 w-full px-3 text-sm outline-none",className)} {...props}/>}
export function Textarea({className,ref,...props}:TextareaHTMLAttributes<HTMLTextAreaElement>&{ref?:React.Ref<HTMLTextAreaElement>}){return <textarea ref={ref} className={cn("control min-h-24 w-full px-3 py-2 text-sm outline-none",className)} {...props}/>}
export function Field({label,required,children,hint,error,composite=false}:{label:string;required?:boolean;children:React.ReactNode;hint?:string;error?:string;composite?:boolean}){const {locale}=useI18n();const content=<><span className="mb-1.5 block text-sm font-semibold text-[var(--ink-700)]">{translateEnglish(label,locale)}{required&&<span className="text-[var(--danger)]" aria-hidden="true"> *</span>}</span>{children}{error?<span className="mt-1.5 block text-xs font-semibold text-[var(--danger)]" role="alert">{translateEnglish(error,locale)}</span>:hint&&<span className="mt-1.5 block text-xs leading-5 text-[var(--ink-500)]">{translateEnglish(hint,locale)}</span>}</>;return composite?<div className="block">{content}</div>:<label className="block">{content}</label>}
