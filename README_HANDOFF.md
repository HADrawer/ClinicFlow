# ClinicFlow Codex Handoff

Copy all files and the hidden `.agents` directory into the root of the ClinicFlow repository.

Then start a new Codex session from the repository root and paste the content of:

`Codex_Implementation_Prompt.md`

Codex reads `AGENTS.md` automatically. The prompt also asks it to read the full system specification and use the two repository skills.
